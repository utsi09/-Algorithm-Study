올바른 괄호처럼 스택을 이용해 푸는 문제이다.

현재 값보다 작거나 같은 값은 pop하면서 처리하고, 같은 값인 경우에는 cnt로 같은 값을 몇 개 들고 있는지 관리한다.

ret에는 현재 값보다 작거나 같은 값을 만날 때마다 top 값의 개수만큼 더해준다.

마지막에 더 큰 값이 남아 있다면 그 값은 항상 한 번 더 볼 수 있기 때문에 ret++을 해준다.
